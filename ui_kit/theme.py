"""Visual constants: colors, fonts, sizes.

Port from: constants.py (the visual bits -- BG_COLOR, board colors,
SQUARE_SIZE, offsets, fonts). Non-visual config goes to shell/config.py.
"""
from __future__ import annotations

# TODO: colors, SQUARE_SIZE, board offsets, font choices, highlight colors
